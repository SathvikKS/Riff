module.exports = async (client, Discord, playlist, message) => {
    const emb = new Discord.MessageEmbed()
    .setColor(client.color.green)
    .setDescription('Starting to parse the playlist');
    //await message.channel.send(emb);
}